Updated todo list

問題二是核心平台層的重構，風險較高。而且問題一已把 `TenantContext` 下沉到 common，可能已改變了依賴數字。我先**重新實測目前的循環依賴現況**，再決定如何安全拆解。 

Ran terminal command:  cd /home/kevin/workspaces/side-project/IoT-forge/backend/src/main/java/com/taipei/iot && mods=(auth tenant audit setting config user dept rbac notification); for m in $mods; do echo "=== $m depends on ==="; grep -rhoE "import com\.taipei\.iot\.[a-z]+" "$m" 2>/dev/null | sed -E 's/import com\.taipei\.iot\.//' | grep -vxF "$m" | sort | uniq -c | sort -rn | tr '\n' ' '; echo; done

問題二是核心平台層重構，牽涉廣、風險中。在動手前我必須先看清「每條環的實際 import 是什麼」，才能找出最小、最安全的切點。讓我逐條盤點。 

Ran terminal command:  echo "### auth -> tenant (7)"; grep -rhE "import com\.taipei\.iot\.tenant" auth | sort | uniq -c; echo; echo "### tenant -> auth (8)"; grep -rhE "import com\.taipei\.iot\.auth" tenant | sort | uniq -c; echo; echo "### audit -> auth (3)"; grep -rhE "import com\.taipei\.iot\.auth" audit | sort | uniq -c; echo; echo "### audit -> tenant (5)"; grep -rhE "import com\.taipei\.iot\.tenant" audit | sort | uniq -c; echo; echo "### config -> auth (3)"; grep -rhE "import com\.taipei\.iot\.auth" config | sort | uniq -c; echo; echo "### auth -> config (1)"; grep -rhE "import com\.taipei\.iot\.config" auth | sort | uniq -c; echo; echo "### setting -> tenant (3)"; grep -rhE "import com\.taipei\.iot\.tenant" setting | sort | uniq -c; echo; echo "### tenant -> setting (3)"; grep -rhE "import com\.taipei\.iot\.setting" tenant | sort | uniq -c

問題一已使 `TenantContext` 下沉，循環數字已下降但仍存在。我先量測「tenant 共用基礎設施型別」的影響範圍——這是打斷多條環的關鍵（`TenantAware`、`TenantScopedRepository` 等屬於 shared kernel，不該在業務模組）。 

Ran terminal command:  echo "=== tenant 套件內的型別清單（找出哪些是 shared-kernel 基礎設施）==="; ls tenant/*.java | xargs -n1 basename; echo; for t in TenantAware TenantEntityListener TenantScopedRepository RunInSystemTenantContext TenantEnabledCache TenantEntity TenantRepository; do c=$(grep -rl "import com.taipei.iot.tenant.$t;" --include=*.java . | wc -l); echo "$t: 被 $c 個檔案 import"; done

Compacted conversationShared-kernel infra confirmed: `TenantAware`(20), `TenantEntityListener`(20), `TenantScopedRepository`(21), `RunInSystemTenantContext`(3) are pure cross-cutting infrastructure used platform-wide. `TenantEntity`/`TenantRepository`(6) are genuine business types — keep in `tenant`. Let me read the kernel sources before moving them. 

Ran terminal command:  cd /home/kevin/workspaces/side-project/IoT-forge/backend/src/main/java/com/taipei/iot/tenant && for f in TenantAware TenantScopedRepository TenantEntityListener RunInSystemTenantContext TenantEnabledCache; do echo "================= $f.java ================="; cat $f.java; echo; done

Read [](file:///home/kevin/.config/Code/User/workspaceStorage/7634e41f9babc7c26259ebacf2d5d251/GitHub.copilot-chat/chat-session-resources/dbd9a946-3a2e-4e11-b6f5-20a8bd5204e9/toolu_014ehQaQCXTuuiZubHkULCNY__vscode-1782778559215/content.txt#1-1), lines 1 to 400

Now I understand the kernel types:
- `TenantAware`, `TenantScopedRepository` — pure marker interfaces, no tenant-business deps
- `TenantEntityListener` — uses only `TenantAware` + `TenantContext` (already in common)
- `RunInSystemTenantContext` — pure annotation
- `TenantEnabledCache` — depends on `TenantRepository` (business) → **stays in tenant**

All four kernel types are cleanly relocatable. Let me check the `common` package layout and same-package usages inside `tenant` before moving. 

Ran terminal command:  cd /home/kevin/workspaces/side-project/IoT-forge/backend/src/main/java/com/taipei/iot && echo "=== common subpackages ==="; ls -d common/*/ ; echo; echo "=== where TenantContext landed in common ==="; ls common/context/; echo; echo "=== same-package (no-import) refs inside tenant/ to the 4 moving types ==="; for t in TenantAware TenantScopedRepository TenantEntityListener RunInSystemTenantContext; do echo "--- $t (referenced in these tenant/ files, excluding its own def) ---"; grep -rl "\b$t\b" tenant --include=*.java | grep -v "tenant/$t.java"; done

There's already a `common/tenant/` package. Let me see what's in it — that's the natural home. 

Ran terminal command:  echo "=== common/tenant/ ==="; ls common/tenant/; echo; echo "=== common/context/ also ==="; ls common/context/; echo; echo "=== head of TenantSystemContextAspect & TenantFilterAspect & TenantConsistencyValidator (imports) ==="; for f in TenantSystemContextAspect TenantFilterAspect TenantConsistencyValidator; do echo "--- $f ---"; sed -n '1,25p' tenant/$f.java; done

`common/tenant/` already exists (the natural home). Plan for breaking the cycles:

**Move to `common.tenant`:** `TenantAware`, `TenantScopedRepository`, `TenantEntityListener`, `RunInSystemTenantContext` (pure infra). Keep `TenantEntity`/`TenantRepository`/`TenantEnabledCache` in `tenant` (real business types).

This fully breaks **tenant↔setting** and sharply reduces `auth/audit/device/dispatch/...→tenant`. Let me get the exact importer file list so I can update every reference.  

Ran terminal command:  echo "=== unique importer files (any of the 4 types), excluding tenant/ own defs ==="; grep -rl -E "import com\.taipei\.iot\.tenant\.(TenantAware|TenantScopedRepository|TenantEntityListener|RunInSystemTenantContext);" --include=*.java . | sort | sed 's|^\./||'; echo; echo "=== total count ==="; grep -rl -E "import com\.taipei\.iot\.tenant\.(TenantAware|TenantScopedRepository|TenantEntityListener|RunInSystemTenantContext);" --include=*.java . | wc -l

I've finished the investigation. Before touching 44+ files in a change I can't compile locally, here's the precise picture and a phased plan — I want your go-ahead on scope since the cycles split into "cheap & safe" vs "deep & risky".

## Re-measured cycles (after 問題一)

| 環 | 方向/數量 | 成因（實際 import） | 切法 | 風險 |
|---|---|---|---|---|
| **tenant ↔ setting** | 3 / 3 | setting→tenant 全是 `TenantAware`/`TenantEntityListener`/`TenantScopedRepository`（**純基礎設施**） | 下沉這 3 個型別到 `common.tenant` | 低 |
| **config ↔ auth** | 1 / 3 | auth→config 只有 `CorsProperties`；config→auth 是 3 個 security filter（組裝根，合理） | 把 `CorsProperties` 移到 `common` | 低 |
| **auth ↔ tenant** | 7→3 / 8 | 下沉 kernel 後 auth→tenant 只剩 `TenantEntity`/`TenantRepository`/`TenantEnabledCache`；tenant→auth 是 `UserEntity`/`UserRepository`… | 需 **問題三**（User 歸屬）才能斷 | 高 |
| **auth ↔ audit** | 9 / 3 | auth 寫稽核(9)；audit 讀 `UserEntity`(3) | 需 **事件化 audit**（4.2） | 中高 |
| **audit ↔ tenant** | 2 / 2 | audit 讀 `TenantEntity`/`TenantRepository` 取場域名 | 需 **事件化 audit** | 中 |

## 關鍵發現
1. `common/tenant/` 套件**已存在**（裡面有 `TenantScopeJpql` 等）——這 4 個基礎型別的天然歸宿。
2. 下沉 4 個 kernel 型別（`TenantAware`/`TenantScopedRepository`/`TenantEntityListener`/`RunInSystemTenantContext`）會動到 **44 個 import 檔** + 4 個 tenant 內部檔，且 spring-javaformat 的 import 排序需要事後 `./mvnw spring-javaformat:apply` 修正。
3. 此下沉**只完整斷開 tenant↔setting**；其餘深層環（auth↔tenant/audit、audit↔tenant）的反向邊源自 audit 讀 User/Tenant 實體與 auth 寫稽核——那是 **4.2 事件化 audit** 與 **問題三** 的範疇，是更大、會改變執行時行為（同步→AFTER_COMMIT）的工作。

## 建議分段執行

- **2A（本次，低風險）**：下沉 4 個 tenant kernel 型別到 `common.tenant` + 把 `CorsProperties` 移到 `common`。→ 完整斷開 **tenant↔setting** 與 **config↔auth**，並大幅降低 auth/audit/device/dispatch…→tenant 的耦合。
- **2B（後續，中高風險）**：事件化 audit（沿用既有 `VirusScanAuditEvent` 模式），斷 auth↔audit、audit↔tenant。會改 runtime 行為，建議獨立一輪、你能編譯驗證時做。
- **auth↔tenant / tenant↔user 的反向邊**留給**問題三**。

你要我**現在就執行 2A**（48 檔的純搬移＋import 改寫，完成後你跑 `spring-javaformat:apply` 與編譯）嗎？或先只做更小的 `CorsProperties` 那一刀來驗證流程？

Created 3 todos