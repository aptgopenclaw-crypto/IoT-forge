# Allow FreezingArchRule to create/update violation store on first run
# Existing violations are recorded in src/test/resources/archunit/frozen-rules/
# and only NEW violations cause test failure.
freeze.store.default.allowStoreCreation=true
